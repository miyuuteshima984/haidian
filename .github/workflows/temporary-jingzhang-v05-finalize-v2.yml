name: Jingzhang finalize archive

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Archived workflow notice
        run: echo "Jingzhang temporary finalization workflow is archived after v0.6 validation."
