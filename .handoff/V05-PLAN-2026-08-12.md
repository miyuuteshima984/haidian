# 京张城市完整度 v0.5 执行状态

## 目标

- 质量目标：在 #1954 最终 intake 71/100 基础上，针对可由投稿方补齐的任务书显性成果、运营、隐私、品牌、组件、版权与表达完整度进行真实深化；目标为 85+，但以新 Review Agent 实际结果为准。
- 贡献目标：使用真实、独立可审的提交累积 upstream contributions；不使用空 commit、标点拆分或无关修改刷排名。

## 分支

- fork: `miyuuteshima984/haidian`
- upstream: `open-city-ai/haidian`
- v0.5 branch: `submission/miyuuteshima984/jingzhang-city-completeness-v05`
- automation: `automation/jingzhang-finalize`
- package: `submissions/miyuuteshima984/jingzhang-ai-civic-infrastructure/`

## 已完成真实设计 commits

1. `c8540cd7` — taskbook coordination loop
2. `c5c687f8` — full-factor AI ecosystem map
3. `e545f59c` — ten AI scenario cards
4. `290d9f54` — regional collaboration validation matrix
5. `4ae45af2` — implementation and operations matrix
6. `db84327e` — scenario privacy governance matrix
7. `b390b450` — brand and VI concept system
8. `31f7dac8` — public-space components and wayfinding
9. `67188157` — per-asset rights and generation ledger
10. `343693a3` — source provenance and reuse records
11. `1bc35320` — bilingual proposal integration
12. `2e36f2ac` — v0.5 changelog / scope
13. `d7cbc211` — global AI case comparison table
14. `428898e1` — public landmarks, events and conversion pathway

## v0.5 新成果

- 三大定位—五大功能—三区两翼—C7 反馈闭环
- 六个全球 AI 生态案例机制对照表
- 土地/空间/产业/资金/人才/算力/数据/场景全要素生态图谱
- 10 张完整 AI+ 场景卡
- 北纬社区 / 未来科学城 / 怀柔科学城 / 北京经开区 / 京津冀区域协同验证矩阵
- 项目—空间—角色—前置条件—启停阈值—维护—KPI 实施运营矩阵
- 位置/健康/家庭/账号/行为/科研企业数据的场景级隐私治理
- C7 COMPLETE LOOP 原创 VI 概念方向
- 7 类公共空间组件 + L1/L2/L3 三层导视
- Open Test Yard / City Commons Hall / Jing-Zhang Civic Station 三个公共荣誉节点
- C7 audit → Developer Open Week → Controlled Test Days → Public Review / Archive 年度活动
- Public Problem → Translate → Controlled Test → Public Review → Long-Term City Service 转化路径
- 逐资产版权/生成台账
- sources provenance / accessed date / reuse / limitations / collection / transformations

## 当前 final packaging gate

禁止重新运行只允许 scaffold 的 `finalize_submission.py`。

正确顺序：

1. 将最后两组图件（case comparison、landmarks/events/conversion）接入中英文 proposal；
2. 更新 agent.1-agent.6 的 visual evidence 与 taskbook standard summary；
3. 重建 `report/proposal.html` / `.en.html`；
4. 重建 `visual/index.html` / `.en.html`；
5. 重排并生成 A3/A0 中英 PDF；
6. 将 10 组新增 SVG 中英对共 20 个文件登记到 manifest，先提供合法 SHA 字段；
7. `refresh_submission_manifest.py` 写入真实 SHA-256；
8. `self_check_submission.py --mark-self-checked`；
9. PNG Pillow verify/load + SVG XML parse + PDF `pdfinfo`；
10. commit packaging；
11. `participant_preflight.py --check-push`；
12. 仅在全部 PASS 时 push v0.5 branch。

## 已定位并修复的 automation 根因

- 大仓库完整 checkout 过慢 → 改为 blobless + sparse。
- sparse 曾漏掉根目录 `scenarios/`/`tracks/` → validator 误报 scenario registry missing；现已加入。
- 新 SVG manifest 条目曾缺 `sha256` 字段 → canonical refresh 不会凭空添加；现先写 64 位零占位，再由 refresh 写真实哈希。

## 下一步 PR

建议标题：

`迭代方案：京张城市完整度 v0.5 / JING-ZHANG CITY COMPLETENESS`

PR body 已准备：`.handoff/V05-PR-BODY.md`。

开 PR 前必须确认：

- target branch final packaging commit 已 push；
- `self_check.json`: `ok=true`, `can_enter_formal_review=true`, `review_status=formal-review-ready`；
- four local gates PASS；
- `manifest.validation_claim.known_blockers=[]`；
- preflight blockers=[]；
- diff 只在自己的 submission package；
- upstream rule/tool drift 仍无相关变更。

PR 开出后：等待 official submission-validation → Review Agent → maintainer；只处理新 exact head 的真实 blocker / required repair。
